{
    "name": "tic-tac-toe",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "flow-bin": "^0.38.0",
        "gh-pages": "^0.12.0",
        "react-scripts": "0.8.5"
    },
    "dependencies": {
        "react": "^15.4.2",
        "react-dom": "^16.0.1"
    },
    "scripts": {
        "start": "react-scripts start",
        "build": "react-scripts build",
        "test": "react-scripts test --env=jsdom",
        "eject": "react-scripts eject",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "homepage": "https://brentoncozby.github.io/react-tic-tac-toe/"
}
